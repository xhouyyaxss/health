<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hxy.dao.OrderDao" >
    <resultMap id="baseResultMap" type="com.hxy.pojo.Order">
        <id column="id" property="id"/>
        <result column="member_id" property="memberId"/>
        <result column="orderDate" property="orderDate"/>
        <result column="orderType" property="orderType"/>
        <result column="orderStatus" property="orderStatus"/>
        <result column="setmeal_id" property="setmealId"/>
    </resultMap>
    <!--新增-->
    <insert id="add" parameterType="com.hxy.pojo.Order">
        <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into
        t_order
        (member_id,orderDate,orderType,orderStatus,setmeal_id)
        values
        (#{memberId},#{orderDate},#{orderType},#{orderStatus},#{setmealId})
    </insert>

    <!--动态条件查询-->
    <select id="findByCondition"
            parameterType="com.hxy.pojo.Order"
            resultMap="baseResultMap">
        select * from t_order
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="memberId != null">
                and member_id = #{memberId}
            </if>
            <if test="orderDate != null">
                and orderDate = #{orderDate}
            </if>
            <if test="orderType != null">
                and orderType = #{orderType}
            </if>
            <if test="orderStatus != null">
                and orderStatus = #{orderStatus}
            </if>
            <if test="setmealId != null">
                and setmeal_id = #{setmealId}
            </if>
        </where>
    </select>
<!--  通过id查询order并返回需要的字段数据-->
    <select id="findById4Details" parameterType="Integer" resultType="Map">
     select m.name 'member',s.name setmeal,o.orderDate orderDate,o.orderType orderType from t_order o,t_member m,t_setmeal s where o.member_id=m.id and o.setmeal_id=s.id and o.id = #{orderId}
    </select>

    <select id="findCountBySetmealId" parameterType="Integer" resultType="Integer">
    select count(id) from t_order where setmeal_id=#{setmealId}
    </select>
    <select id="findOrderCountByDate" parameterType="String" resultType="Integer">
     select count(id) from t_order where  orderDate = #{date}
    </select>

    <select id="findOrderCountAfterDate" parameterType="String" resultType="Integer">
     select count(id) from t_order where orderDate &gt;= #{date}
    </select>
    <select id="findVisitCountAfterDate" parameterType="String" resultType="Integer">
            select count(id) from t_order where orderStatus = '已到诊' and orderDate &gt;= #{date}
    </select>
    <select id="findVisitCountByDate" parameterType="String" resultType="Integer">
        select count(id) from t_order where orderStatus = '已到诊' and orderDate = #{date}
    </select>

</mapper>